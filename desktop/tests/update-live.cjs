// Real COS check/download/hash verification in Electron, without launching installer.
const {_electron}=require(process.env.CINE_PLAYWRIGHT||'playwright');const path=require('node:path');const fs=require('node:fs/promises');const assert=require('node:assert/strict');
(async()=>{const folder=path.resolve(__dirname,'../test-output/update-live');await fs.mkdir(folder,{recursive:true});const entry=path.join(folder,'entry.cjs');await fs.writeFile(entry,`const {app,BrowserWindow}=require('electron');app.whenReady().then(()=>{new BrowserWindow({show:false}).loadURL('about:blank');global.run=async()=>{const {NsisUpdater}=require(${JSON.stringify(path.resolve(__dirname,'../node_modules/electron-updater'))});const adapter={version:'0.2.0',name:'cine-update-test',isPackaged:true,appUpdateConfigPath:${JSON.stringify(path.resolve(__dirname,'../release/win-unpacked/resources/app-update.yml'))},userDataPath:${JSON.stringify(folder)},baseCachePath:${JSON.stringify(folder)},whenReady:()=>app.whenReady(),onQuit:()=>{},quit:()=>{throw Error('Test must not install');},relaunch:()=>{}};const updater=new NsisUpdater(null,adapter);updater.httpExecutor=new (require(${JSON.stringify(path.resolve(__dirname,'../node_modules/electron-updater/out/electronHttpExecutor.js'))}).ElectronHttpExecutor)(()=>{});updater.logger=null;updater.autoInstallOnAppQuit=false;updater.disableDifferentialDownload=true;updater.on('error',()=>{});const result=await updater.checkForUpdates();const files=await result.downloadPromise;return {version:result.updateInfo.version,files};};});`);const env={...process.env};delete env.ELECTRON_RUN_AS_NODE;const electron=await _electron.launch({executablePath:path.resolve(__dirname,'../node_modules/electron/dist/electron.exe'),args:[entry],env,timeout:30000});try{await electron.firstWindow();const result=await electron.evaluate(()=>global.run());assert.equal(result.version,require('../package.json').version);assert.ok(result.files[0].endsWith('.exe'));console.log('Real Electron updater: older-version detection, COS download, SHA512 validation and ready-to-install passed. Installer was not launched.');}finally{await electron.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
