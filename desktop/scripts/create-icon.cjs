// Render the code-native CineSleuth mark and wrap the PNG in an ICO container.
const {chromium}=require(process.env.CINE_PLAYWRIGHT||'playwright');const fs=require('node:fs/promises');const path=require('node:path');
(async()=>{const root=path.resolve(__dirname,'../build');await fs.mkdir(root,{recursive:true});const browser=await chromium.launch({channel:'msedge',headless:true});
try{const page=await browser.newPage({viewport:{width:256,height:256},deviceScaleFactor:1});await page.setContent('<style>html,body{margin:0;background:transparent}</style><svg xmlns="http://www.w3.org/2000/svg" width="256" height="256" viewBox="0 0 256 256"><rect x="4" y="4" width="248" height="248" rx="52" fill="#17221d"/><path d="M62 67H128V189H62Z" fill="none" stroke="#d5e6a2" stroke-width="13"/><path d="M128 67H194V189H128Z" fill="none" stroke="#81997c" stroke-width="13"/><circle cx="194" cy="67" r="15" fill="#d5e6a2"/></svg>');const png=await page.screenshot({omitBackground:true});await fs.writeFile(path.join(root,'icon.png'),png);const header=Buffer.alloc(22);header.writeUInt16LE(1,2);header.writeUInt16LE(1,4);header.writeUInt16LE(1,10);header.writeUInt16LE(32,12);header.writeUInt32LE(png.length,14);header.writeUInt32LE(22,18);await fs.writeFile(path.join(root,'icon.ico'),Buffer.concat([header,png]));}finally{await browser.close();}})().catch(error=>{console.error(error);process.exitCode=1;});
